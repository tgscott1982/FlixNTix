@model int;

@if (Model > 0)
{
    <li class="nav-item">
        <a class="nav-link text-light" style="font-size:x-large" asp-controller="Order" asp-action="ShoppingCart">
            <i class="bi bi-cart2"></i> @Model
        </a>
    </li>
}